I will implement the export functionality by enhancing the frontend viewer and slightly updating the backend to provide metadata for better filenames.

### Backend Changes
**File:** `paperpilot/routes/agent_routes/agent_summary_route.py`
- Update the `/api/paper/<paper_id>/analysis/result` endpoint to return the paper's `title` in the JSON response. This allows the frontend to generate meaningful filenames (e.g., `Paper_Title_Analysis.md` instead of `analysis.md`).

### Frontend Changes
**File:** `templates/analysis_viewer.html`
1. **UI Updates**:
   - Add an "Export" dropdown menu or separate buttons ("Export Markdown", "Export PDF") to the header.
2. **Export Logic**:
   - **Markdown Export**: Implement `exportMarkdown()` using the modern `window.showSaveFilePicker` API (with a fallback to standard download). This explicitly triggers a system dialog for the user to select the storage path and filename, satisfying the "confirm storage path" requirement.
   - **PDF Export**: Implement `exportPDF()` using `window.print()`. This leverages the browser's native rendering engine to "Save as PDF".
     - **Why this approach**: This is the most robust way to ensure that complex elements like MathJax formulas, syntax highlighting, and charts/images are rendered exactly as seen on screen, fulfilling the "display normally" requirement without heavy backend dependencies.
3. **Print Styling**:
   - Add `@media print` CSS rules to hide the header, buttons, and scrollbars, and ensure the content area is properly formatted for A4 paper (removing shadows, adjusting margins, enabling background graphics for code blocks).

### Verification Plan
- **Markdown**: Click export, verify the "Save As" dialog appears, save the file, and check that the content matches the viewer.
- **PDF**: Click export (or print), select "Save as PDF", and verify the output PDF contains correct images, formatting, and no UI clutter.