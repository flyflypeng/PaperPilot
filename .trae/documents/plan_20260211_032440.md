I will implement the export feature by modifying the frontend code to expose the existing backend export APIs.

### Backend (Already Implemented)
The backend already provides the necessary APIs in `agent_summary_route.py`:
- `GET /api/paper/<paper_id>/analysis/export/markdown`: Exports the raw Markdown file.
- `GET /api/paper/<paper_id>/analysis/export/pdf`: Generates and exports a PDF file.
  - This endpoint handles image embedding (converting images to base64) to ensure charts display correctly.
  - It uses `PyMuPDF (fitz)` for conversion, which is confirmed to be installed in the environment.

### Frontend Implementation
I will modify the following files to add "Export Markdown" and "Export PDF" buttons:

1.  **`static/js/app.js`** (Main Interface)
    -   Update `viewAnalysisResult` function to inject the export buttons into the AI interpretation toolbar.
    -   Add a helper function `exportAnalysis(paperId, format)` to trigger the download.
    -   The buttons will use the browser's native download behavior (triggering the "Save As" dialog or downloading to the default folder, depending on user browser settings).

2.  **`templates/analysis_viewer.html`** (Full-screen Viewer)
    -   Update the header section to include the export buttons.
    -   Use the `{{ paper_id }}` context variable to construct the export URLs.

### Verification
-   I will verify the changes by checking the updated code logic.
-   (Optional) If possible, I will perform a dry-run or mock test to ensure the buttons call the correct URLs.
